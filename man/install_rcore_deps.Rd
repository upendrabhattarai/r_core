% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install_deps.R
\name{install_rcore_deps}
\alias{install_rcore_deps}
\title{Install rcore template dependencies}
\usage{
install_rcore_deps(type = "core", ask = TRUE, upgrade = "never")
}
\arguments{
\item{type}{Character; which set of dependencies to install.
\code{"core"} installs only the lightweight packages needed to load rcore
itself.  \code{"all"} installs every package used across all templates.
Named analysis types install only what that workflow needs:
\code{"rnaseq"}, \code{"chipseq"}, \code{"singlecell"}, \code{"spatial"},
\code{"methylation"}, \code{"multiomics"}, \code{"cellchat"}.
Default: \code{"core"}.}

\item{ask}{Logical; passed to \code{\link[BiocManager:install]{BiocManager::install()}}. Set \code{FALSE} for
non-interactive / scripted installs.  Default \code{TRUE}.}

\item{upgrade}{Character; passed to \code{\link[BiocManager:install]{BiocManager::install()}}.
\code{"never"}, \code{"default"}, or \code{"always"}. Default \code{"never"}.}
}
\value{
Invisibly returns a list with elements \code{cran}, \code{bioc}, and
\code{github} listing what was attempted.
}
\description{
Installs all R packages required to run rcore report templates.
Handles CRAN, Bioconductor, and GitHub-only packages automatically.
}
\examples{
\dontrun{
  # Minimal install (just rcore itself works)
  install_rcore_deps()

  # Install everything needed for RNA-seq reports
  install_rcore_deps("rnaseq")

  # Install everything needed for single-cell reports
  install_rcore_deps("singlecell")

  # Install ALL dependencies (takes a while)
  install_rcore_deps("all")
}
}
